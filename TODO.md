# TODO

Always read `AGENTS.md` for general info on the project.

Look at the project in detail. We want to add the feature to allow for system member specific display names. For that we want to first add an UI to view system members (basic name and SP id suffices). Then we want to add a config to set a members display name.

# STEPS

...
